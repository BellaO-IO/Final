@*@{
    ViewData["Title"] = "The Top Apps";
}
<h1>@ViewData["Title"]</h1>

<p>The Top Apps go here.</p>
<table class="table" style="table-layout: fixed">
    <tr>
        <th> Rank  </th>
        <th> Title </th>
        <th> Number of Reviews </th>
    </tr>
    @{
        foreach (var apps in TopChart)
        {
            <tr>
                <th>@{$"{apps.rank}"} </th>
                <th>@{$"{apps.title}"} </th>
                <th>@{$"{apps.rating_count}"} </th>
                
            </tr>
        }
    }

    <





</table>
    @{
        //i can use this to put the Topapps in
    }*@
